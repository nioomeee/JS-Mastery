let calculator = {
    add : function(a, b)
    {
        return a+b;
    }, 

    sub: function(a, b)
    {
        return a-b;
    },
    
    mul: function(a,b)
    {
        return a*b;
    },

    div: function(a, b)
    {
        return a/b;
    }
};

let a = parseInt(prompt("Enter 1st number:"));
let b = parseInt(prompt("Enter 1st number:"));

