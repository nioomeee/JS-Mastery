// Create a simple module with two functions (e.g., add() and subtract()), export them, and import them in a separate file to use.

export function add(a, b)
{
    return a+b;
}
export function sub(a, b)
{
    return a-b;
}