let n = parseInt(prompt("Enter the number of elements to be printed: "));

let colors= ["red", "yellow", "pink", "purple", "green", "blue"];

for(let i = 0; i < n; i ++)
{
    console.log(colors[i]);
}
